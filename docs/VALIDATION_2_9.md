# Проверка версии 2.9

Минимальная приёмка:

```bash
pytest -q tests
python -m tools.healthcheck --app-root . --data-dir data
node --check web/frontend/assets/operations-state.js
node --check web/frontend/assets/operations-ui.js
```

Ручные сценарии:

1. первичная настройка администратора;
2. создание оператора и роли просмотра;
3. CSV-импорт с изменением сопоставления и отменой до записи;
4. создание второй ревизии шаблона, сравнение и откат;
5. сохранение второго компонента по открытому файлу;
6. формирование расписания и открытие истории;
7. скачивание сохранённого результата;
8. повтор запуска из истории;
9. проверка журнала действий.
