# 📅 Планировщик Расписания на базе OR-Tools (Сводное расписание)

Система автоматизированного составления и агрегации учебных расписаний. В данный момент фокус направлен на трансформацию групповых расписаний в сводный отчет по преподавателям.

## 🚀 Текущий статус
- [x] Анализ структуры входных и выходных Excel-файлов.
- [x] Проектирование модели данных `Lesson`.
- [x] Реализация загрузчика данных из Excel (`src/data_loader.py`).
- [x] Реализация трансформации данных для сводного отчета (`src/transformer.py`).
- [x] Реализация экспорта в Excel (`src/exporter.py`).
- [x] Создание точки входа `main.py` для генерации отчета.
- [x] Начало разработки веб-интерфейса (FastAPI + Vue.js).

## 🛠 Технологический стек
- **Язык:** Python 3.11+
- **Библиотеки:** openpyxl, pandas, FastAPI, Pydantic
- **Frontend:** Vue.js 3, Bootstrap 5 (Offline mode)

## 🏗 Структура проекта
- `src/` — исходный код системы.
  - `data_loader.py` — чтение и парсинг входных Excel-файлов.
  - `transformer.py` — агрегация данных по преподавателям.
  - `exporter.py` — генерация сводного Excel-отчета.
- `web/` — веб-сервис.
  - `backend/` — API на FastAPI.
  - `frontend/` — интерфейс пользователя.
- `main.py` — основной скрипт запуска.
- `obrazec/` — примеры входных и выходных файлов.
- `teachers.json` — конфигурация преподавателей.
- `plan.md` — детальный план разработки.

## 🚦 Как запустить
### CLI версия
Для генерации сводного расписания выполните:
```bash
python3 main.py
```
Результат будет сохранен в `output/result.xlsx`.

### Web версия (в разработке)
```bash
uvicorn web.backend.main:app --reload
```

## ⚖️ Ограничения и планы
- [x] Реализация базовых unit-тестов.
- [x] Тонкая настройка стилей Excel (согласно `obrazec/Сводное расписание семестр.xlsx`).
- [x] Обработка совмещенных занятий (лекций на поток).
- [ ] Реализация полноценного веб-интерфейса для управления преподавателями и загрузки файлов.